package com.example.springbootdemo;

/*import com.example.springbootdemo.mq.java.FanoutSender;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.text.SimpleDateFormat;
import java.util.Date;

@RunWith(SpringRunner.class)
@SpringBootTest
public class FanoutTest extends SpringbootdemoApplicationTests {
    @Autowired
    private FanoutSender sender;

    @Test
    public void Test() throws InterruptedException{
        SimpleDateFormat sf = new SimpleDateFormat("yyyy-MM-dd");
        sender.send("Time1 =>"+sf.format(new Date()));
        sender.send2("Time2 =>"+sf.format(new Date()));
    }

    //  可以看出fanout会把消息分发到所有订阅到该交换机的队列，fanout模式是忽略路由键的
}*/
